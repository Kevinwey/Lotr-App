import styled from "styled-components";
import Link from "next/link";

const StyledVolumeLink = styled(Link)`
  font-family: var(--font-family);
  font-weight: 500;
  font-size: 1rem;
  color: var(--color-earth);
  text-decoration: none;
  width: 140px;
`;

export default StyledVolumeLink;

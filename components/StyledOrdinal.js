import styled from "styled-components";

const StyledOrdinal = styled.p`
  font-family: var(--font-family);
  font-style: italic;
  font-weight: 400;
  font-size: 0.7rem;
  color: var(--color-clouds);
`;

export default StyledOrdinal;
